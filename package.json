{
  "name": "multaiplayer",
  "private": true,
  "version": "0.1.0-alpha.0",
  "description": "Private group chat for coding with Codex. Open source.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "npm run build -w @multaiplayer/protocol && npm run build -w @multaiplayer/crypto && npm run build -w @multaiplayer/codex && npm run build -w @multaiplayer/git && npm run build -w @multaiplayer/github",
    "build:apps": "npm run build -w @multaiplayer/relay && npm run build -w @multaiplayer/desktop",
    "check": "npm run build:packages && npm run check --workspaces --if-present",
    "test": "npm run build:packages && node --test scripts/*.test.mjs && npm run test --workspaces --if-present",
    "fmt:rust:check": "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml -- --check",
    "test:native": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "verify": "npm run check && npm test && npm run fmt:rust:check && npm run test:native && npm run build",
    "release:preflight": "npm run doctor && npm run verify",
    "doctor": "node scripts/doctor.mjs",
    "doctor:production-relay": "node scripts/doctor.mjs --production-relay",
    "dev": "concurrently -k --names relay,desktop --prefix-colors cyan,green \"npm:dev:relay\" \"npm:dev:desktop\"",
    "dev:desktop": "npm run dev -w @multaiplayer/desktop",
    "dev:relay": "npm run dev -w @multaiplayer/relay",
    "tauri:dev": "npm run tauri:dev -w @multaiplayer/desktop",
    "tauri:build": "npm run tauri:build -w @multaiplayer/desktop"
  },
  "devDependencies": {
    "concurrently": "^9.2.3",
    "typescript": "^5.9.3"
  }
}
