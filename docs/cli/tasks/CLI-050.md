# CLI-050: Relay transport and workspace reads

State: complete

## Outcome

Provide authenticated relay HTTP and WebSocket transport with strict parsing,
ordered handling, acknowledgements, bounded reconnect, and read-only workspace
listing.

## Allowed scope

- CLI/client-core relay transport and tests.
- No relay server behavior change.

## Acceptance criteria

- [ ] Session/header/subprotocol origin binding matches desktop behavior.
- [ ] Messages are processed serially and invalid server data closes safely.
- [ ] Join/publish acknowledgements and reconnect errors are deterministic.
- [ ] Teams and rooms list without introducing client-side defaults drift.

## Verification

- Mock relay ordering, malformed-message, ack-timeout, and reconnect tests.
- Existing relay tests.
