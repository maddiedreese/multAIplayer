# CLI-070: Room creation and local project association

State: paused_waiting_on_CLI-040-R1

## Outcome

Let an authenticated CLI user list, create, and open a room, create its MLS
group, become active host, and bind a host-local project without sharing its path.

## Allowed scope

- CLI room commands and reusable room orchestration.

## Acceptance criteria

- [ ] Room records remain compatible with desktop defaults and validation.
- [ ] The host path is local-only and canonically validated.
- [ ] Room/MLS creation is recoverable and idempotent.
- [ ] Unsupported host handoff remains unavailable explicitly.

## Verification

- Headless create/open/restart journey with a real relay fixture.
- Path validation and rollback tests.
