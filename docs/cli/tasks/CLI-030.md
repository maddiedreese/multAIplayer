# CLI-030: Rust protocol types and golden fixtures

State: complete

## Outcome

Represent the current relay and room wire records strictly in Rust and prove
TypeScript/Rust serialization compatibility without changing the wire contract.

## Allowed scope

- New Rust protocol code and shared compatibility fixtures.
- Additive TypeScript fixture exporters/tests that do not change behavior.

## Acceptance criteria

- [ ] Current records round-trip identically across languages.
- [ ] Bounds and unknown-field behavior match the TypeScript authority.
- [ ] Unsupported records fail or degrade exactly as documented.
- [ ] No relay schema or version change.

## Verification

- Cross-language golden fixture suite.
- Malformed and oversized input property tests.
