# CLI-020: Inert Rust CLI scaffold

State: complete

## Outcome

Add an independently locked, unpublished Rust application at `apps/cli` whose
installed binary is `multAIplayer` and which supports only bounded help/version
output.

## Allowed scope

- `apps/cli/**`
- CLI-only validation commands and docs authorized by CLI-010.

## Acceptance criteria

- [ ] Separate Cargo manifest and lockfile.
- [ ] No npm package and no desktop workspace membership.
- [ ] `multAIplayer --help` and `--version` work.
- [ ] No relay, MLS, authentication, or product behavior yet.
- [ ] Desktop Cargo.lock and packaging are unchanged.

## Verification

- CLI fmt, clippy, and test commands.
- Desktop release-isolation check.
