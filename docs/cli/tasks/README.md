# CLI task specifications

Each file is a bounded implementation unit. Its Codex task must begin by reading
the repository instructions, approved plan, decisions, status, and its own file.
Every task waits for explicit owner approval before implementation. Approval of
one task does not authorize another task or broaden its allowed scope.

