# CLI-110: UI-independent Codex host extraction

State: waiting_for_owner_approval

## Outcome

Extract the existing app-server process, compatibility, JSON-RPC, cancellation,
thread, and projection behavior behind UI-independent interfaces consumed by
thin Tauri and CLI adapters.

## Allowed scope

- Additive Codex host crate/module and behavior-preserving desktop adapter.
- Focused native tests and existing compatibility fixtures.

## Acceptance criteria

- [ ] Desktop behavior and security invariants remain unchanged.
- [ ] Core has no Tauri, React, browser, Zustand, or terminal dependency.
- [ ] Sensitive upstream fields remain discarded.
- [ ] Unknown requests and compatibility gaps fail closed.
- [ ] Existing Codex native tests and journeys pass.

## Verification

- Desktop native Codex suite and generated-schema compatibility fixtures.
- New adapter-equivalence tests.

