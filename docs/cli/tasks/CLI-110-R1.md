# CLI-110-R1: Shared MLS fuzz lockfile reconciliation

State: active

## Outcome

Reconcile the checked-in `mls-core` fuzz workspace lockfile with the already
accepted manifests so locked dependency-policy verification and both native
fuzz targets execute reproducibly, without changing dependency intent,
cryptographic policy, wire behavior, or desktop behavior.

## Dependencies

- CLI-010-R1 complete and integrated.
- CLI-110 preserved at merge head
  `b7e269e43546563fac0caf1c0515360246d9a14b` while this correction runs.

## Allowed scope

- Update only
  `apps/desktop/src-tauri/crates/mls-core/fuzz/Cargo.lock` from the already
  accepted manifests.
- Review and report the exact dependency-resolution delta.

## Protected scope

- Every Cargo manifest and source file.
- MLS cryptographic policy, wire formats, serialization, and dependency intent.
- CLI and desktop behavior.
- Desktop workspace `Cargo.lock` and all updater, signing, notarization,
  versioning, release workflow, and release asset surfaces.
- Approved plan, locked decisions, and unrelated governance or product paths.

## Acceptance criteria

- [ ] The sole task diff is the authorized fuzz `Cargo.lock`.
- [ ] The lockfile is generated from the accepted manifests without manual
      package selection or manifest change.
- [ ] The exact added, removed, and updated dependency delta is reviewed and
      contains no unexplained or unaccepted source.
- [ ] Locked advisories and sources checks pass without mutating the worktree.
- [ ] Both existing full-duration native fuzz targets execute with their
      unchanged targets and arguments.
- [ ] Full MLS, CLI, and desktop verification passes without skips.
- [ ] Classification, release-isolation, protected-path, diff, ancestry, and
      repository-cleanliness audits pass.

## Required verification

- Locked dependency advisories and sources audit against the fuzz manifest.
- Exact lockfile package/source/license delta review.
- Both existing native fuzz targets for their full configured duration.
- Complete `mls-core`, locked CLI, and desktop native/frontend suites.
- Applicable protocol and relay compatibility checks exercised by those suites.
- Change classification and desktop release-isolation tests.
- Exact changed-path and protected desktop/release-surface audit.
- `git diff --check`, clean final worktree, and exact commit ancestry.

## Stop conditions

Stop and report to CLI-000 if resolution requires a manifest or source change,
introduces an unexplained dependency or source, changes cryptographic or wire
policy, changes desktop behavior, touches a protected release surface, or if any
required audit, test, fuzz target, or cleanliness check fails, flakes, skips, or
does not execute.
