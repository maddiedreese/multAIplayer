# Compatibility and migration inventory

This is the repository-wide inventory of code that intentionally reads an older persisted, wire, or upstream shape. Product security claims remain in the [threat model](threat-model.md). A row may be removed only when its removal condition is met and the associated test is changed in the same pull request.

“Reject guard” means the old format is recognized only to produce a safe error; it is not accepted as current data. Additive normalizers may fill documented non-secret defaults, but no reader may silently accept an obsolete cryptographic wire format.

| Boundary | Current and supported legacy input | Reader or migration | Owner | Regression evidence | Removal condition |
| --- | --- | --- | --- | --- | --- |
| Codex app-server manifests | Contract-tested Codex `0.133.0` through `0.144.0`; newer versions run with security-affecting capabilities disabled and a warning | `scripts/codex-compatibility.mjs`; `apps/desktop/src-tauri/src/codex_account/compatibility.rs` | Codex host integration | `apps/desktop/test/onboardingReadiness.test.ts`; `apps/desktop/src-tauri/src/codex_account/tests.rs`; scheduled live contract check | Raise the lower bound only after the supported app release no longer needs the old manifest and the public support range is updated. Newer-version restriction is permanent fail-closed behavior, not a temporary migration. |
| Relay storage backend | SQLite is current; one configured legacy JSON v1 store may be imported once, renamed, and never used again | `apps/relay/src/sqlite-persistence.ts` | Relay persistence | `apps/relay/test/persistence/state.test.ts` legacy import, preservation, invalid input, and unsupported-version cases | Remove after a published deprecation window and evidence that supported hosted/self-hosted installs have completed the one-way migration. |
| Relay document rows | Store document v1; missing fields from earlier v1 rows receive explicitly documented safe defaults | `apps/relay/src/store-codec-migrations.ts`; `apps/relay/src/store-codec-normalizers.ts` | Relay persistence | `apps/relay/test/store-persistence.test.ts`; `apps/relay/test/persistence/state.test.ts` | Individual defaults may be removed only after the maximum supported backup horizon no longer contains the old row shape. A future document version requires a new explicit branch. |
| Removed host-local room configuration | Legacy plaintext room configuration fields may exist in SQLite rows, pages, or WAL; they are purged and the database is compacted | `apps/relay/src/sqlite-state-repository.ts` | Relay persistence/security | `apps/relay/test/persistence/state.test.ts` | Remove only after every supported database and restorable backup predates the field removal and the threat model no longer requires historical purge. |
| Relay authentication sessions | Legacy plaintext session identifiers are converted to digests; legacy token-bearing properties are discarded | `apps/relay/src/auth/session.ts` | Relay authentication | `apps/relay/test/auth/session-persistence.test.ts`; `apps/relay/test/auth/session.test.ts` | Remove after the longest supported database/backup retention window cannot contain plaintext session identifiers. |
| Desktop first-run state | Onboarding state v1 is the public baseline; malformed and unsupported versions are discarded | `apps/desktop/src/lib/onboarding/onboardingState.ts` | Desktop onboarding | `apps/desktop/test/onboardingState.test.ts` | Add a migration only after a public release creates an installed state that must survive an upgrade. |
| Device-local display history | History payload v3 is the public baseline; older, unversioned, and malformed payloads are rejected | `apps/desktop/src/lib/history/localRoomHistoryPayload.ts` | Desktop history | `apps/desktop/test/codexActivity.test.ts`; `apps/desktop/test/codexThreadGraph.test.ts` | Keep current-version validation and bounds permanently; add migrations only for public formats. |
| Encrypted room archives | Archive envelope v1 and embedded history v1 are the only accepted versions; all other versions are rejected | `apps/desktop/src-tauri/src/room_archive/codec.rs`; `apps/desktop/src-tauri/src/room_archive.rs` | Native archive | Native `room_archive` tests, including version/bounds/symlink rejection | Reject guards are permanent until a deliberate new archive version adds an explicit reader. Do not reinterpret v1. |
| Invitation links | Only current approval-gated fragment invites are accepted; query-string ids, secret-style links, duplicate keys, and unknown fields are invalid | `apps/desktop/src/lib/onboarding/onboardingInviteJoin.ts`; `apps/desktop/src-tauri/src/invite_link.rs` | Invite onboarding/native link parser | `apps/desktop/test/onboardingInviteJoin.test.ts`; `apps/desktop/test/inviteUrl.test.ts`; native invite-link tests | Preserve strict parsing and fragment scrubbing; add compatibility only for links issued by a public release. |
| Native MLS identity store | Identity store v1 is current; other versions and inconsistent identity/device records are rejected | `apps/desktop/src-tauri/src/mls_native/store_support.rs` | Native MLS state | `apps/desktop/src-tauri/src/mls_native/tests.rs` | Permanent reject guard until a new explicit, tested, atomic native migration exists. |
| MLS GroupContext policy | GroupContext extension v2 is emitted; v1 remains accepted for groups created by the immediately preceding protocol generation | `apps/desktop/src-tauri/crates/mls-core/src/policy.rs` | MLS core | `apps/desktop/src-tauri/crates/mls-core/src/engine/tests.rs`; lifecycle/security-state-machine integration tests | Remove v1 only when no supported room can still carry it and removal has a tested user recovery path. |
| Directed invite/HPKE envelopes | Invite binding v3 and sealed payload v1 are current; malformed or other versions are rejected rather than migrated | `apps/desktop/src-tauri/src/mls_native/invites.rs`; `apps/desktop/src-tauri/crates/mls-core/src/hpke_seal.rs`; protocol validators | MLS core/protocol | `apps/desktop/src-tauri/src/mls_native/tests.rs`; `apps/relay/test/opaque.test.ts`; directed-invite fuzz corpus | Permanent reject guards until a deliberately versioned protocol transition is specified and tested end-to-end. |
| Codex catalog projection | Current service-tier field is preferred; the prior speed field is read as a display/catalog fallback | `apps/desktop/src-tauri/src/codex_catalog.rs` | Codex catalog | `codex_catalog::tests::catalog_normalization_preserves_legacy_speed_mapping` | Remove after the minimum supported Codex version no longer emits the legacy field and the recorded compatibility manifest proves it. |
| Native command errors | Structured command errors are current; an internal legacy string is wrapped with a stable fallback code | `apps/desktop/src-tauri/src/command_error.rs` | Native IPC | `command_error::tests::legacy_string_conversion_has_a_stable_fallback_code` | Remove when all native call sites return structured errors and a repository search plus tests prove no string conversion remains. |

## Representation rule

`tsconfig.base.json` enables `strict`, `noUncheckedIndexedAccess`, and `exactOptionalPropertyTypes`. Wire and persistence normalizers omit absent properties; `null` is used only for an explicit protocol value. A decoded `undefined` must be reconstructed away before serialization. Purely in-memory display/callback types may use `optional?: T | undefined`, but those types must not be reused as persistence or wire contracts.

When adding a compatibility path, add its row, a bounded fixture or test, and a concrete removal condition in the same change. When removing one, include the search/test evidence that the condition has been met.
